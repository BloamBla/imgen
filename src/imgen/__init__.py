"""imgen — local image generation CLI for Apple Silicon Macs.

Two modes since v0.7.0: text-to-image (`imgen draw`) via FLUX.1-dev,
and photo restyle (`imgen generate` / `imgen batch`) via FLUX.1-Kontext
or Qwen-Image-Edit. All on-device via mflux + MLX.
"""

__version__ = "0.7.1"
